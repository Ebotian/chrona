文章详情页(内容展示)设计:
左上方提供背景透明,边框文字均为#ef475d的圆角矩形返回按钮,hover提供背景色#ffa0ac

主要功能:md渲染,github风格为基础,代码块内采用等宽字体,带语法高亮,主题solarized-light,支持图片展示,支持表格展示,支持数学公式展示(latex格式)

在src/components/home/TagRegion.vue里点击相应标题之后:标题平滑移动到该模块范围顶部(时间轴不随之移动,全局时间此时被锁定只读,也不响应各种移动行为,直到手动点击退出/回到主界面),文章到底之后下方有一条和标题横线一样长的横线表示文章已经结尾.每篇文章应该有单独的路由

渲染区域的背景是透明的,文章在标题平滑移动结束之后渐变出现